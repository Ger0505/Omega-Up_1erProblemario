#include <stdio.h>
#include <math.h>
int main(){
	int A,B;
	do{
		scanf("%i %i",&A,&B);
	}while(A<1 | A>2*pow(10,9)| B<1 | B>2*pow(10,9));
	if((A*2)==B | (B*2)==A ){
		printf("felizmil");
	}else{
		printf("tristemil");
	}
}
